# YouTube Data API v3 事前準備・環境構築マニュアル

本書は、PythonプログラムからYouTubeへ動画を自動アップロード（限定公開）するために必要なGoogle Cloud Consoleでの設定手順およびPython環境構築の手順書です。

---

## 1. 前提条件

- **Googleアカウント**（動画をアップロードしたいYouTubeチャンネルに紐づくアカウント）
- **Python 3.8 以降**がインストールされている環境

---

## 2. Python環境のセットアップ

動画アップロードプログラムに必要な公式ライブラリをインストールします。端末（ターミナルまたはコマンドプロンプト）を開き、以下のコマンドを実行してください。

```bash
pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib
```

---

## 3. Google Cloud Consoleでの事前設定

YouTube APIを利用するための「クライアント秘密鍵（`client_secret.json`）」を発行します。

### Step 1: プロジェクトの作成
1. [Google Cloud Console](https://console.cloud.google.com/) にアクセスし、Googleアカウントでログインします。
2. 画面上部のプロジェクト選択ドロップダウンをクリックし、**「新しいプロジェクト」** を選択します。
3. プロジェクト名（例: `YouTube-Uploader`）を入力し、**「作成」** ボタンをクリックします。

### Step 2: YouTube Data API v3 の有効化
1. 左側のナビゲーションメニューから **「APIとサービス」 > 「ライブラリ」** を開きます。
2. 検索バーに `YouTube Data API v3` と入力して検索します。
3. 検索結果から **「YouTube Data API v3」** を選択し、**「有効にする」** ボタンをクリックします。

### Step 3: OAuth 同意画面の設定
1. 左側メニューの **「APIとサービス」 > 「OAuth 同意画面」** を開きます。
2. User Type で **「外部」**（External）を選択し、**「作成」** をクリックします。
3. 以下の必要項目を入力します：
   - **アプリ名**: 例）`YouTube Video Uploader`
   - **ユーザーサポートメール**: 自身のメールアドレス
   - **デベロッパーの連絡先情報**: 自身のメールアドレス
4. **「保存して次へ」** をクリックします。
5. **スコープ（Scopes）の設定**:
   - そのまま「保存して次へ」をクリックします（コード側で指定します）。
6. **テストユーザー（Test Users）の設定**:
   - **「+ ADD USERS」** をクリックし、動画をアップロードするYouTubeアカウント（Googleアドレス）を追加します。
   - ※アプリを公開審査に出さない場合、登録されたテストユーザーのみがAPIを利用可能です。
7. 入力完了後、**「保存して次へ」** をクリックして設定を完了します。

### Step 4: OAuth 2.0 クライアント ID の発行
1. 左側メニューの **「APIとサービス」 > 「認証情報」** を開きます。
2. 画面上部の **「+ 認証情報を作成」** をクリックし、**「OAuth クライアント ID」** を選択します。
3. アプリケーションの種類で **「デスクトップ アプリ」**（Desktop app）を選択します。
4. 名前（例: `YouTube Upload Client`）を入力し、**「作成」** をクリックします。
5. 作成完了ダイアログが表示されたら、**「JSON をダウンロード」** をクリックします。
6. ダウンロードしたファイルを `client_secret.json` というファイル名に変更し、**実行するPythonスクリプトと同じディレクトリ（フォルダ）** に配置してください。

---

## 4. プログラム実行手順と初回認証

### 初回実行時の動作
1. 設定完了後、Pythonプログラムを実行します。
2. ブラウザが自動的に立ち上がり、Googleアカウントのログイン・選択画面が表示されます。
3. **Step 3で登録したテストユーザーのアカウント**を選択します。
4. 「このアプリは Google で確認されていません」という警告画面が出た場合：
   - **「詳細」**（Advanced）をクリックします。
   - **「（アプリ名）に移動（安全ではないページ）」** をクリックして進みます。
5. アクセス許可（YouTubeアカウントへのアクセス権限）の確認画面が出たら **「続行」** をクリックします。
6. 認証が成功すると、同じフォルダ内に `token.json` が自動作成されます。

### 2回目以降の実行
- `token.json` が存在するため、ブラウザでの認証画面はスキップされ、バックグラウンドで自動的にアップロード処理が実行されます。

---

## 5. 注意事項・トラブルシューティング

- **APIクォータ（利用制限）について**
  - YouTube Data API v3 のデフォルト無料枠は **1日あたり 10,000 クォータ** です。
  - 動画のアップロード（`videos.insert`）は1回につき **約1,600 クォータ** を消費します（1日あたり約6本までアップロード可能）。
  - クォータ上限を超えると `quotaExceeded` エラーが発生するため注意してください。

- **`403 Access Not Configured` エラーが発生する場合**
  - Google Cloud Consoleで「YouTube Data API v3」が正しく有効化されているか確認してください。

- **`403 Disallowed User` エラーが発生する場合**
  - OAuth同意画面の「テストユーザー」に、使用しているGoogleアカウントが正しく登録されているか確認してください。

- **認証をやり直したい場合**
  - フォルダ内の `token.json` ファイルを削除してプログラムを再実行してください。再度ブラウザ認証が行われます。